---
description: 项目上下文维护完成后自动发布到 AI Code Review Assistant（覆盖 Gainode 与创世DOG 两个工作区）
alwaysApply: true
---

# 自动发布项目上下文

## 触发时机

每次完成任一已登记项目 `.project-ai` 下任何上下文文档的维护/修改后，**无需等待用户再次确认**，自动执行发布流程。

## 项目登记（project_id → 工作区 → 项目名）

| project_id | 工作区根目录 | 项目名 |
|---|---|---|
| 8 | `E:\github\sports` | Gainode |
| 2 | `E:\github\AItradeos\创世狗` | 创世DOG |

根据当前工作区根目录自动匹配 project_id 与项目名，不要混用。

## 发布流程

1. 调用 `user-ai-code-review` MCP 的 `complete_project_context_update`（传入匹配的 `project_id`）。这会创建并发布新的不可变上下文版本，并在后台启动 ChatGPT 同步。
2. 轮询 `get_project_context_status`（传 `project_name`），直到任一 `chatgpt_web` 绑定报告 `SYNCED` 或 `FAILED`。

## 已知工具副作用（必须处理）

`complete_project_context_update` 会把 `manifest.yaml` 缩减为最小版本，删除 `decisionSources`、`STAGE_*` 状态字段、`governanceBaseline*`、`note` 等治理数据（具体损失范围取决于该项目 manifest 原有字段）。

**发布后必须恢复 `manifest.yaml`**：保留工具正确写入的 `contextVersion` 和 `status`（发布后应为 `PUBLISHED`），同时还原其余全部治理字段（`STAGE_*`、完整 `excludedPaths`、`governanceBaselineCommit`、`governanceBaseline`、`projectionBuiltAt`、`note`、`decisionSources` 全部条目等，以该项目发布前的 manifest 为准）。

## 同步失败处理

若 `sync_status = failed` 且 `last_error = "无法向 ChatGPT 写入一次性审核契约。"`，这是浏览器/ChatGPT 会话问题（共享浏览器中的审核标签页已关闭或失效），**发布本身已成功**。向用户报告并请其在共享浏览器恢复 ChatGPT 审核会话；若用户指示「继续」，则跳过同步门禁继续后续流程，无需重复发布。
