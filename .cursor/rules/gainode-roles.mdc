---
description: Gainode 多角色协作与独立审核执行纪律（五角色职责分离、双流水线、提交来源分离、门禁由 Quality 把关）
alwaysApply: true
---

# Gainode 角色执行纪律

> 权威规则见 `.project-ai/rules/roles.md`、`.project-ai/rules/workflow.md`、
> `.project-ai/rules/git-review-worktree.md`。
> 冲突时以权威规则 + `通过agent开发前规则/MASTER_PROJECT_GOVERNANCE.md` +
> `Gainode_Development_Ready_V6.1_Latest/07_DEVELOPMENT_AND_ACCEPTANCE.md`（V3.4）为准。

## 五角色职责分离（硬约束）

| 角色 | 定位 | 关键边界 |
| --- | --- | --- |
| Owner（人工） | 唯一最终决策者 | 批准范围/提交/推送/合并/发布/资产 |
| Scheduler | 策划拆解派发 | 不写业务代码，不做 git 写操作 |
| Developer | 唯一执行者，一开到底 | 只本地 commit，禁止 push/merge/rebase/cherry-pick |
| Quality | 独立内审 + 提审 + 合并唯一执行者 | 唯一可 push/提审/merge，仍需 Owner 授权 |
| Reviewer | 只读外审 | 只给 Verdict + 证据，不改文件 |

计划、实现、质量控制、外部审核必须职责分离；参与实现的 Agent 不得代替独立 Reviewer 给外审结论。

## 核心纪律

1. **Developer 一开到底**：完成一个包后立即生成快照并继续下一个已定义包，不等待审核结论、
   合同冻结、Owner 决策或 Stage Gate 关闭（对应 07 V3.4 `DEV_GATE_MODEL`）。
2. **Developer 禁止**：`push / merge / rebase / cherry-pick / tag / release / deploy`。
3. **Quality 是唯一可 push/提审/merge 的 Agent**；执行仍需 Owner 明确授权（「唯一执行者」≠「自动获权」）。
4. **提交来源分离**：Developer 提交 trailer 用 `Code-Origin: Developer` + `Git-Operator: Developer`；
   Quality 修复用 `Code-Origin: Quality` + `Review-Findings:`；二者禁止混入同一 commit，禁止 amend/squash/rebase 抹边界。
5. **门禁（11 项）由 Quality 在审核/提审/合并环节把关**，不阻塞 Developer 推进后续无依赖 Stage。
6. **遇范围扩大/合同冲突/Owner 选择**：只登记 `NEEDS_OWNER_DECISION` 并继续无依赖部分，不中断整体进度。

## 独立审核 worktree

- 审核对象 = 固定 SHA 的独立 worktree（分支 `gainode/review/<stage-id>`，base=`master`），物理隔离，不碰 Developer HEAD。
- 建立/回滚命令见 `.project-ai/rules/git-review-worktree.md`。
